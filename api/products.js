const axios = require('axios')

const getAllProducts = async () => {
    config = {
        method: 'GET',
        url: 'https://intergrationstore.myshopify.com'
    }
}

module.exports = {
    getAllProducts
}